/**
 * Notification Service
 * Purpose: Send WhatsApp notifications to coaches and students (Phase 2)
 */

class NotificationService {
  async sendCoachNotification(coachId, type, data) {
    // Phase 2: Send via WhatsApp
    return null;
  }

  async sendStudentNotification(studentPhone, message) {
    // Phase 2: Send via WhatsApp
    return null;
  }
}

module.exports = new NotificationService();
